#include <stdlib.h>
#include <assert.h>
#include <stdio.h>



int main(){

	int arg = 0;
	assert(arg); // se arg == 0, l'asserzione fallisce e il programma viene arrestato. unmessaggio viene stampato in stderr
	return 0;
}
